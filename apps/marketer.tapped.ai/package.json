{
  "name": "marketer.tapped.ai",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R public/. dist/"
  }
}
